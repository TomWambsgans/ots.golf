[project]
name = "ots-golf-service"
version = "0.1.0"
description = "ots.golf: the site, the webhook and the verification worker"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "sqlalchemy>=2.0",
  "jinja2>=3.1",
  "httpx>=0.27",
  "markdown>=3.6",
  "nh3>=0.2.18",
]

[tool.uv]
package = false
