import OptimalOTS.WholeWords

/-! Rendered by the verifier with the submission's claim. Sources are 128-bit words;
only concatenation and selecting a fixed half of a 256-bit hash are deterministic operations.
The bare-oracle DAG security experiment and all size and cost limits are unchanged. -/

namespace OptimalOTS.Challenge.DisclosureLower

/-- Every weakly secure whole-word DAG has an index whose verification costs at least
`{{CLAIM}}` compressions. -/
theorem candidate : WholeWordVerificationLowerBound paperParams {{CLAIM}} := sorry

end OptimalOTS.Challenge.DisclosureLower
